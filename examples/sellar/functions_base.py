# -*- coding: utf-8 -*-
"""
  functions_base.py generated by WhatsOpt 1.10.4
"""
# DO NOT EDIT unless you know what you are doing
# whatsopt_url: https://ether.onera.fr/whatsopt
# analysis_id: 1


import numpy as np
from numpy import nan
from os import path
from importlib import import_module
from yaml import load, FullLoader
from openmdao.api import ExplicitComponent

class FunctionsBase(ExplicitComponent):
    """ An OpenMDAO base component to encapsulate Functions discipline """

    def __init__(self, **kwargs):
        super(FunctionsBase, self).__init__(**kwargs)
        self._impl = None
        dockconf = path.join(path.dirname(__file__), ".whatsopt_dock.yml")
        if path.exists(dockconf):
            with open(dockconf) as dockfile:
                dock = load(dockfile, Loader=FullLoader)
                impl = dock.get("functions")
                if impl:
                    module = import_module(impl['module'])
                    self._impl = getattr(module, impl['class'])()

    def setup(self):
        self.add_input('x', val=2, desc='')
        self.add_input('y1', val=1.0, desc='')
        self.add_input('y2', val=1.0, desc='')
        self.add_input('z', val=[5, 2], desc='')

        self.add_output('f', val=1.0, desc='')

        self.add_output('g1', val=1.0, desc='')

        self.add_output('g2', val=1.0, desc='')



        